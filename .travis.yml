language: java
jdk:
  - oraclejdk8
  - openjdk8
dist: trusty

script: mvn test verify install sonar:sonar -Dsonar.projectKey=lecousin_java-compression -B 
  
after_success:
 - bash <(curl -s https://codecov.io/bash)
